json.extract! chamado, :id, :fornecedor, :cliente, :site, :endereco, :bairro, :cep, :cidade, :estado, :tecnico, :data, :numero, :nfornecedor, :ncliente, :observacao, :created_at, :updated_at
json.url chamado_url(chamado, format: :json)
